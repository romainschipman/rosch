import { Meta, Canvas, Story, Controls } from '@storybook/blocks';
import * as RoschHeaderStories from './rosch-header.stories';

<Meta of={RoschHeaderStories} />

# Header


The Header component defines the table headers. It requires the columnNames prop to map the column names to their respective display labels.

## Props
<Controls of={RoschHeaderStories.DefaultCell} />

---